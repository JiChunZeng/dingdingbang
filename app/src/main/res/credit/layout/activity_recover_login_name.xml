<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="recoverLoginNameActivity"
            type="com.dingtaifu.dingdingbang.mvp.credit.view.RecoverLoginNameActivity" />
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/bottom_background"
        android:orientation="vertical"
        tools:context=".mvp.credit.view.RecoverLoginNameActivity">

        <include
            android:id="@+id/layout_common_title"
            layout="@layout/layout_common_title" />

        <include
            android:id="@+id/layout_recover_name_id"
            layout="@layout/layout_recover_name_id" />

    </LinearLayout>
</layout>
